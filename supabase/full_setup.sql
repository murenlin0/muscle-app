-- =============================================================================
-- full_setup.sql — 全新資料庫建立順序（說明檔）
-- =============================================================================
-- Supabase SQL Editor 一次只能貼一個腳本，請依序執行：
--
--   1. 00_reset.sql      ← ⚠️ 清空所有業務資料
--   2. 01_schema.sql     ← 核心表結構
--   3. 02_rls.sql        ← 權限與 RLS
--   4. reconcile.sql     ← 增量 + 種子 + 驗證（可重複跑）
--
-- 已有資料的正式環境：只跑 reconcile.sql 即可。
-- =============================================================================

select '請依序執行 00 → 01 → 02 → reconcile，不要只跑本檔' as notice;
